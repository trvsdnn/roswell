<div class='page-header'>
  <h2>
    <%= @note.title %>
    <% if @note.tags.present? %><small><%= @note.tags.join(' ') %></small><% end %>
    <%= link_to 'Edit', edit_note_path, :class=> 'btn btn-small pull-right' %>
  </h2>
</div>

<div class="well">
  <%= @note.body %>
</div>
